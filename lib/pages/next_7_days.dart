import 'package:client/types/next_days_page_arguments.dart';
import 'package:flutter/material.dart';

class Next7Days extends StatelessWidget {
  final NextDaysPageArguments arguments;

  const Next7Days({Key? key, required this.arguments}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
